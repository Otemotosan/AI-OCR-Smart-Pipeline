# Environment template for UI development
# Copy this to .env.local and modify as needed

# API URL - leave empty to use Vite proxy (localhost:8080)
# Or set to staging/production API URL for direct testing
VITE_API_URL=
